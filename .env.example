# Desktop runtime shell is auto-detected in most environments.
# Add project-level env vars here if future desktop features require them.

# Example:
# AGENT_OBSERVER_LOG_LEVEL=info
# AGENT_OBSERVER_INSTALL_BEACON_URL=https://agentobserver.dev/api/install-beacon
